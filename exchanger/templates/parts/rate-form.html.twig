<div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.currency) }}
        </div>
        <div class="form-group">
            {{ form_row(form.rate) }}
        </div>
        {{ form_row(form.submit) }}
        {{ form_end(form) }}
    </div>
    <div class="col-md-4"></div>
</div>
